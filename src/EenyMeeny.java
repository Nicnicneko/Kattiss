import java.util.*;
import java.io.*;

public class EenyMeeny {
    public static void main(String[] args) throws IOException {
        Scanner nya = new Scanner(System.in);

        int rhy = nya.nextLine().split(" ").length, len = Integer.parseInt(nya.nextLine());

        ArrayList<String> peep = new ArrayList<>(), a = new ArrayList<>(), b = new ArrayList<>();
        boolean cool = false;

        for (int i = 0; i < len; i++) {
            peep.add(nya.nextLine());
        }

        for(int i = 0; !peep.isEmpty();  ){

        }
    }
}
